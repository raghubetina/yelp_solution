<div class="page-header">
  <h1>
    Responses
    <a href="/responses/new" class="btn btn-lg btn-success">New Response</a>
  </h1>
</div><div class="row mb-1">
  <div class="col-md-12">
    <%= search_form_for @q, :class => "collapse", :id => "responses_filters" do |f| %>
      <p class="lead">Narrow results:</p>

        <div class="form-group">
    <%= f.label :body_cont, "Body contains" %>
    <%= f.text_field :body_cont, :class => "form-control", :placeholder => "Body contains" %>
  </div>

<div class="form-group">
          <%= f.label :created_at, "Created at" %>
          <div class="row">
            <div class='col-md-6 mb-1'>
              <div class='input-group date' id='created_atgteqPicker'>
                  <%= f.text_field :created_at_gteq, :class => "form-control", :placeholder => "Created at greater than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>

            <div class='col-md-6'>
              <div class='input-group date' id='created_atlteqPicker'>
                  <%= f.text_field :created_at_lteq, :class => "form-control", :placeholder => "Created at less than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>
          </div>
      </div>
      <script type="text/javascript">
          $(function () {
              $('#created_atgteqPicker').datetimepicker({
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#created_atlteqPicker').datetimepicker({
                  useCurrent: false,
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#created_atgteqPicker').on("dp.change", function (e) {
                  $('#created_atlteqPicker').data("DateTimePicker").minDate(e.date);
              });
              $('#created_atlteqPicker').on("dp.change", function (e) {
                  $('#created_atgteqPicker').data("DateTimePicker").maxDate(e.date);
              });
          });
      </script>

        <div class="form-group">
          <%= f.label :created_at, "Ownership Created at" %>
          <div class="row">
            <div class='col-md-6 mb-1'>
              <div class='input-group date' id='ownershipgteqcreated_atPicker'>
                  <%= f.text_field :ownership_created_at_gteq, :class => "form-control", :placeholder => "Ownership Created at greater than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>

            <div class='col-md-6'>
              <div class='input-group date' id='ownershiplteqcreated_atPicker'>
                  <%= f.text_field :ownership_created_at_lteq, :class => "form-control", :placeholder => "Ownership Created at less than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>
          </div>
      </div>
      <script type="text/javascript">
          $(function () {
              $('#ownershipgteqcreated_atPicker').datetimepicker({
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#ownershiplteqcreated_atPicker').datetimepicker({
                  useCurrent: false,
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#ownershipgteqcreated_atPicker').on("dp.change", function (e) {
                  $('#ownershiplteqcreated_atPicker').data("DateTimePicker").minDate(e.date);
              });
              $('#ownershiplteqcreated_atPicker').on("dp.change", function (e) {
                  $('#ownershipgteqcreated_atPicker').data("DateTimePicker").maxDate(e.date);
              });
          });
      </script>

<div class="form-group">
      <%= f.label :review_body_cont, "Review body contains" %>
      <%= f.text_field :review_body_cont, :class => "form-control", :placeholder => "Review body contains" %>
    </div>

      <%= f.submit :class => "btn btn-primary btn-block mb-1" %>

      <a href="/responses" class="btn btn-default btn-block mb-1">Clear filters</a>
    <% end %>
    <a class="btn btn-default btn-block mb-1" data-toggle="collapse" href="#responses_filters">
      Show Filters
    </a>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Review</th>
        <th>Ownership</th>
        <th>Body</th>
        <th>Actions</th>
      </tr>

      <% @responses.each do |response| %>
      <tr>
        <td>
          <% if response.review.present? %>
            <a href="/reviews/<%= response.review.id %>">
              <%= response.review.body %>
            </a>
          <% end %>
        </td>
        <td>
          <% if response.ownership.present? %>
            <a href="/ownerships/<%= response.ownership.id %>">
              <%= response.ownership.created_at %>
            </a>
          <% end %>
        </td>
        <td><%= response.body %></td>
        <td>
          <a href="/responses/<%= response.id %>" class="btn btn-primary">Show</a>
          <a href="/responses/<%= response.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_response/<%= response.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
    <%= paginate @responses, theme: 'twitter-bootstrap-4' %>
  </div>
</div>
